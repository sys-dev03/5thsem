Q2->
pgsql--
create database student;
\c student;
create table Teacher (TNo int primary key, TName varchar(20), Subject varchar(20));

cmd--
javac -cp.; driver Main.java
java -cp.; driver Main

Q1->
cmd--
javac Main.java
java Main